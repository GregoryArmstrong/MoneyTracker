<h1>Update Transaction</h1>
<%= form_for([@category, @transaction]) do |f| %>

  <%= f.label :amount %>
  <%= f.text_field :amount %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit "Update Transaction" %>
<% end %>
